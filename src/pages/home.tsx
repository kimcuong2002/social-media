import { Button } from '@chakra-ui/react';

const Home = () => {
  return (
    <div className="mt-36">
      <span>Home</span>
      <Button colorScheme="blue">Save</Button>
      <Button>Cancel</Button>
    </div>
  );
};

export default Home;
