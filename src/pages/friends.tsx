import { FriendSideBar } from '@/features';
import NavbarLayout from '@/layout/navbar-layout';

export const Friends = () => {
  return <NavbarLayout navBarChildren={<FriendSideBar />}>fsndn</NavbarLayout>;
};
