import { Box } from '@chakra-ui/react';

import NavbarLayout from '@/layout/navbar-layout';

export const Event = () => {
  return (
    <NavbarLayout navBarChildren={<Box>sdfbashfbs</Box>}>Event</NavbarLayout>
  );
};
